name: Fix map GeoJSON paths

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fix-map-data-paths.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix-paths:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Replace relative GeoJSON paths with GitHub Raw URLs
        shell: python
        run: |
          from pathlib import Path

          path =